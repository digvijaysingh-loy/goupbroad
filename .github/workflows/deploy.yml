name: Deploy to VPS Server

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H 82.29.164.111 >> ~/.ssh/known_hosts

      - name: Deploy to Server
        env:
          DEPLOY_PATH: /home/GoAbroad
          DEPLOY_HOST: 82.29.164.111
          DEPLOY_USER: root
          GITHUB_USERNAME: EasyTechInnovate
          GITHUB_REPO: GoAbroad
          GITHUB_TOKEN: ${{ secrets.DEPLOY_GITHUB_TOKEN }}
        run: |
          ssh $DEPLOY_USER@$DEPLOY_HOST "
            export DEPLOY_PATH='$DEPLOY_PATH'
            export GITHUB_USERNAME='$GITHUB_USERNAME'
            export GITHUB_REPO='$GITHUB_REPO'
            export GITHUB_TOKEN='$GITHUB_TOKEN'
            
            # Create directory if it doesn't exist
            mkdir -p '$DEPLOY_PATH'
            
            # Initialize git repository if it doesn't exist
            cd '$DEPLOY_PATH'
            if [ ! -d .git ]; then
              git init
              git remote add origin https://$GITHUB_USERNAME:$GITHUB_TOKEN@github.com/$GITHUB_USERNAME/$GITHUB_REPO.git
            else
              git remote set-url origin https://$GITHUB_USERNAME:$GITHUB_TOKEN@github.com/$GITHUB_USERNAME/$GITHUB_REPO.git
            fi

            # Fetch and reset to match remote
            git fetch origin main
            git reset --hard origin/main

            # Continue with deployment...
            if [ ! -f "docker-compose.prod.yml" ]; then
              echo '❌ Error: docker-compose.prod.yml not found'
              exit 1
            fi

            # Stop existing containers FIRST to free up port 80
            echo '🛑 Stopping existing containers...'
            docker-compose -f docker-compose.prod.yml down || true
            
            # Wait a moment for ports to be released
            sleep 5

            # SSL Certificate Setup with Certbot (now that port 80 is free)
            echo '🔒 Checking/Updating SSL certificates...'
            certbot certonly --standalone -d qa.goupbroad.com --non-interactive --agree-tos -m goupbroad0907@gmail.com || { echo '❌ Certbot failed'; exit 1; }

            # Build and deploy with Docker Compose
            echo '🏗️ Building and deploying with Docker Compose...'
            docker-compose --env-file .env.production -f docker-compose.prod.yml up -d --build || { echo '❌ Docker Compose failed'; exit 1; }

            # Clean up old Docker resources
            echo '🧹 Cleaning up Docker resources...'
            docker system prune -f || { echo '⚠️ Docker prune failed'; exit 1; }

            echo '✨ Deployment completed successfully!'
          "

      - name: Verify Deployment
        run: |
          echo "⏳ Waiting for services to start..."
          sleep 30
          
          echo "🔍 Verifying service availability..."
          curl -f -s -S --retry 3 --retry-delay 5 -I http://82.29.164.111:80 || {
            echo "❌ Service verification failed"
            exit 1
          }